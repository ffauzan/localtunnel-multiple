{
  "name": "localtunnel-docker",
  "version": "1.0.0",
  "description": "Docker image for localtunnel client",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "serve": "node server.js"
  },
  "author": "Falih",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1",
    "localtunnel": "^2.0.2",
    "yaml": "^1.10.2"
  }
}
